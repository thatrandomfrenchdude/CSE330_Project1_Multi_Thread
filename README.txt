This program creates five sample threads and runs them one at a time with a global variable to illustrate multi-threading.thread_test.c contains the main functions along with the five sample threads.

q.h contains the queue functions.

tcb.h contains the thread control block functions.

threads.h contains the thread functions.